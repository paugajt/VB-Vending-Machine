<%= form_for(section) do |f| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :course_id %><br>
      <%= f.collection_select(:course_id, @courses, :id, :name, include_blank: true) %>
    </div>

  <div class="field">
    <%= f.label :section_number %>
    <%= f.number_field :section_number %>
  </div>

  
  <div class="field">
    <%= f.label :building %>
    <%= f.text_field :building %>
  </div>

  <div class="field">
    <%= f.label :room_number %>
    <%= f.number_field :room_number %>
  </div>

  <div class="field">
    <%= f.label :professor_id %><br>
    <%= f.collection_select(:professor_id, @professors, :id, :name, include_blank: true) %>
  </div>




  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
